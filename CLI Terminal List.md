npx quartz build = Generete web look dari notenya(Wajib sebelum commit)
git add . = menambah semua file yang di edit ke daftar file yang akan di push ke git.
git commit -m "isi pesan commit" = mengcommit ke git.
git push = push perubahan ke git.